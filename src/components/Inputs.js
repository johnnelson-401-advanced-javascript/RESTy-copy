import React, { Component } from 'react';
import PropTypes from 'prop-types';

export default Input = ({ method, myFunc }) => {


    const buttons = [{}]
    return (
       
       <>
  <form>
        <input type='text' name='url-input'></input>
        <label>
          {radio && <input name='method' type="radio" value={text}>{text}</input>}
          {button && <button type="submit">Go</button>}
        </label>

  </form>
        </>
        )
        }
      }
    };
    
ButtonBar.propTypes = {
          ButtonBar: PropTypes.array.isRequired
      };
